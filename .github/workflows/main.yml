name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a one-line script
        run: |
          mkdir a
          mkdir a/.platform
          mv .platform.app.yaml a
          mv routes.yaml a/.platform
          mv services.yaml a/.platform
          mv bb a
          mv ss a
          mv npm a
      - name: push repo
        run: 
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git commit -m "Update test.txt"
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: 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
          branch: main
#          chmod +x aa
#          chmod +x ss
#          chmod +x bb
#          docker login -u christiankm01 -p Danang-112
#          docker build . -t christiankm01/kilo:latest 
#          docker push christiankm01/kilo:latest

          
