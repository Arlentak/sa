name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: adam7/platformsh-cli-action@v1.1
        with: 
          token: SYMo0x9e30A6pZHJo-tNTH_wmdgUntze2f6hSTnqYXY
      - run: |
         platform
         platform get rzxkqzyrmckqq -y
         ls

          
